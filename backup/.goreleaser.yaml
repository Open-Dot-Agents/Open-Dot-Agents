version: 2

project_name: dota

snapshot:
  version_template: "{{ .Version }}"

builds:
  - id: dota-bundle
    dir: cli
    main: ./cmd/dota
    binary: dota
    env: [CGO_ENABLED=0]
    goos: [darwin, linux, windows]
    goarch: [amd64, arm64]
    ldflags: ["-s -w -X main.version={{ .Tag }}"]
    mod_timestamp: "{{ .CommitTimestamp }}"
  - id: dota-adapter-codex-bundle
    dir: cli
    main: ./cmd/dota-adapter-codex
    binary: dota-adapter-codex
    env: [CGO_ENABLED=0]
    goos: [darwin, linux, windows]
    goarch: [amd64, arm64]
    ldflags: ["-s -w -X main.version={{ .Version }}"]
    mod_timestamp: "{{ .CommitTimestamp }}"
  - id: dota-adapter-copilot-bundle
    dir: cli
    main: ./cmd/dota-adapter-copilot
    binary: dota-adapter-copilot
    env: [CGO_ENABLED=0]
    goos: [darwin, linux, windows]
    goarch: [amd64, arm64]
    ldflags: ["-s -w -X main.version={{ .Version }}"]
    mod_timestamp: "{{ .CommitTimestamp }}"
  - id: dota-adapter-claude-bundle
    dir: cli
    main: ./cmd/dota-adapter-claude
    binary: dota-adapter-claude
    env: [CGO_ENABLED=0]
    goos: [darwin, linux, windows]
    goarch: [amd64, arm64]
    ldflags: ["-s -w -X main.version={{ .Version }}"]
    mod_timestamp: "{{ .CommitTimestamp }}"
  - id: dota-adapter-codex
    dir: cli
    main: ./cmd/dota-adapter-codex
    binary: dota-adapter-codex
    env: [CGO_ENABLED=0]
    goos: [darwin, linux, windows]
    goarch: [amd64, arm64]
    ldflags: ["-s -w -X main.version={{ .Version }}"]
    mod_timestamp: "{{ .CommitTimestamp }}"
  - id: dota-adapter-copilot
    dir: cli
    main: ./cmd/dota-adapter-copilot
    binary: dota-adapter-copilot
    env: [CGO_ENABLED=0]
    goos: [darwin, linux, windows]
    goarch: [amd64, arm64]
    ldflags: ["-s -w -X main.version={{ .Version }}"]
    mod_timestamp: "{{ .CommitTimestamp }}"
  - id: dota-adapter-claude
    dir: cli
    main: ./cmd/dota-adapter-claude
    binary: dota-adapter-claude
    env: [CGO_ENABLED=0]
    goos: [darwin, linux, windows]
    goarch: [amd64, arm64]
    ldflags: ["-s -w -X main.version={{ .Version }}"]
    mod_timestamp: "{{ .CommitTimestamp }}"

archives:
  - id: bundle
    ids:
      - dota-bundle
      - dota-adapter-codex-bundle
      - dota-adapter-copilot-bundle
      - dota-adapter-claude-bundle
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
  - id: adapter-codex
    ids: [dota-adapter-codex]
    formats: [binary]
    name_template: "{{ .Binary }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
  - id: adapter-copilot
    ids: [dota-adapter-copilot]
    formats: [binary]
    name_template: "{{ .Binary }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
  - id: adapter-claude
    ids: [dota-adapter-claude]
    formats: [binary]
    name_template: "{{ .Binary }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: checksums.txt
  algorithm: sha256

sboms:
  - artifacts: archive
    documents:
      - "{{ .ArtifactName }}.spdx.sbom"

changelog:
  use: git
  sort: asc

release:
  draft: true
  name_template: "Open-Dot-Agents {{ .Tag }}"
  header: |
    Open-Dot-Agents {{ .Tag }} is a release candidate of the vendor-neutral
    `.agents/` v1 standard, the `dota` trusted host, and the external Codex,
    Copilot, and Claude reference adapters.
  footer: |
    This is a clean break from v0.0.1. Verify `checksums.txt` and the SPDX SBOM
    before installing executables. See `COMPATIBILITY.md` for the exact native
    acceptance boundary.
