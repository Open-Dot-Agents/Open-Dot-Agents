# Open-Dot-Agents v1 conformance fixtures

These fixtures are normative inputs for `dota conformance tree`. `valid/minimal`
exercises the smallest Core tree, `valid/full` exercises all thirteen
categories, and `invalid/` contains one stable failure per contract boundary.
Third-party processors and adapters may consume these files without using the
Go implementation.
